## 1. 报告流程测试契约

- [x] 1.1 更新报告验收流程断言：验证 Markdown 结构、顺序、负载值、事件格式和可选校验反馈，不匹配本地化标题。
- [x] 1.2 运行受影响的报告流程测试，并保留下载元数据和结构化报告输入的覆盖。

## 2. 缺失密钥验证

- [x] 2.1 隔离配置医生测试的环境密钥和 `config.local.yaml`，保留必需密钥缺失时退出码 `1`。
- [x] 2.2 让 DashScope 运行器包装脚本在可选 Shell 专属配置解析前到达缺失密钥路径，保留退出码 `2`。
- [x] 2.3 为缺失密钥和提供密钥行为增加或调整聚焦的跨平台包装脚本测试。

## 3. 验证与状态

- [x] 3.1 使用工作区本地临时目录运行聚焦的报告、配置和 DashScope 测试组，并保留现有条件跳过。
- [x] 3.2 使用工作区本地临时目录运行全量测试，并将结果记录到 `docs/DEVELOPMENT_STATUS.md`。
